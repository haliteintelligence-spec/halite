[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @halite/db db:generate",
  "pnpm --filter @halite/dashboard build"
]

[start]
cmd = "pnpm --filter @halite/dashboard start"
